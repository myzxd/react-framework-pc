/**
 * 共享登记 - 印章创建
 */
import React from 'react';
import SealForm from './form';

const Create = ({
  location,
}) => {
  return (
    <SealForm location={location} />
  );
};

export default Create;
